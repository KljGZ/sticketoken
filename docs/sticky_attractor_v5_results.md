# Mode 3 V5 formal-result record

This file is intentionally a registered result surface rather than a pre-run claim. The formal run must update it only after all 150 task/length validations, the validation freeze, the one-time IID test, OOD evaluation, conditional single-poison retrieval, full result inventory, release upload, and fresh-clone recovery audit have completed.

Required final fields are: immutable implementation commit; config, split, model, tokenizer, and dependency hashes; P1/P2/P3 length frontiers; shortest Level-1 and Level-2 candidates; frozen centers/radii/outlier rules; no-refit IID/OOD results; downstream status; query budget; remote/local/GitHub file-count, byte-count, and content-root SHA-256 equality; and direct links to all release shards and recovery instructions.
