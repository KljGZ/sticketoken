# Mode 3 V4 results

This document is populated only from the audited V4 result bundle after all 120 task-by-length validations complete. Until then, no V4 discovery claim is made.

Required evidence: resolved configuration, environment/model revision, leakage audit, query ledgers, complete 4×30 length frontier, frozen trigger and center hash, one-time IID test, fixed-center OOD result, gated one-poison retrieval result when eligible, figures, SHA-256 manifest, and the V4 scope/threat-model audit.
